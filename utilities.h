#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int Gcd(int, int);
long int longgcd(long int, long int);
int expmod(int, int, int);
int longexpmod(int, long int, int);
int perfectSquare(int);
char mrWitness(int, int, int, int);
char mrTest(int);
int *Atkin(int);